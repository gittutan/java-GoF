# 模块名称

适配器模式 (Adapter Pattern)

# 核心概念 (针对Java初学者)

**什么是适配器模式？**

适配器模式（Adapter Pattern）是一种结构型设计模式，它的主要目的是**将一个类的接口转换成客户端期望的另一个接口**。这使得原本由于接口不兼容而不能一起工作的那些类可以一起工作。

可以把它想象成一个“中间人”或“翻译官”，它帮助两个接口不同的类进行有效的沟通。

**类比：电源适配器**

一个非常形象的例子就是我们日常生活中用到的电源适配器。比如，你有一个欧标的电器插头，但墙上的插座是美标的。这时，你就需要一个电源适配器。

*   **欧标插头 (Adaptee - 被适配者)**: 这是已经存在的、接口不符合我们当前需求的类。
*   **美标插座 (Client - 客户端)**: 这是我们的系统，它期望一个特定类型的接口（美标插孔）。
*   **电源适配器 (Adapter - 适配器)**: 它的一端可以插入欧标插头，另一端可以插入美标插座。它将欧标插头的“接口”转换成了美标插座“期望的接口”。

适配器模式在软件中的作用与此类似，它解决的是软件组件之间接口不兼容的问题。

# 适配器模式的两种实现方式

适配器模式主要有两种实现方式：

1.  **类适配器模式 (Class Adapter Pattern)**: 通过**继承**被适配者类，并**实现**目标接口来完成适配。
2.  **对象适配器模式 (Object Adapter Pattern)**: 通过**组合**（持有一个被适配者类的实例）并**实现**目标接口（或继承目标抽象类）来完成适配。这种方式也常被称为**委托 (Delegate)**，因为适配器会将客户端的请求委托给被适配者对象来处理。

接下来我们将分别详细分析这两种实现方式。

# 类适配器模式 (Class Adapter)

类适配器模式使用类的继承机制来实现。适配器类继承自被适配的类，并同时实现目标接口。

**代码分析 (`Adaptor/Extends/`目录下)**

*   **`Banner.java` (被适配者 - Adaptee)**:
    *   这是一个已经存在的类，拥有一些特定的功能，但其接口与客户端期望的不一致。
    *   它有一个构造函数 `public Banner(String string)` 用于接收一个字符串。
    *   它提供了两个方法：
        *   `public void showWithParen()`: 用圆括号 `()` 包裹字符串并打印。
        *   `public void showWithAster()`: 用星号 `*` 包裹字符串并打印。
    *   这个类无法直接满足 `Print` 接口的要求。

*   **`Print.java` (目标接口 - Target)**:
    *   这是客户端期望使用的接口。它定义了客户端希望调用的方法。
    *   它包含两个抽象方法：
        *   `public abstract void printWeek();`: 期望以“弱”方式打印。
        *   `public abstract void printStrong();`: 期望以“强”方式打印。

*   **`PrintBanner.java` (适配器 - Adapter)**:
    *   这是适配器类，它的作用是连接 `Banner` 类和 `Print` 接口。
    *   **实现方式**:
        *   `public class PrintBanner extends Banner implements Print`
            *   它继承了 `Banner` 类，因此获得了 `Banner` 类的方法（`showWithParen` 和 `showWithAster`）。
            *   它实现了 `Print` 接口，因此必须提供 `printWeek` 和 `printStrong` 方法的具体实现。
    *   构造函数 `public PrintBanner(String string)`: 调用父类 `Banner` 的构造函数 `super(string);` 来初始化 `Banner` 部分。
    *   **核心适配方法**:
        *   `public void printWeek()`:
            *   这个方法是 `Print` 接口中定义的方法。
            *   它的实现 `showWithParen();` 调用了继承自 `Banner` 类的 `showWithParen()` 方法。这样，客户端调用 `printWeek()` 时，实际执行的是 `Banner` 的 `showWithParen()` 功能。
        *   `public void printStrong()`:
            *   这个方法也是 `Print` 接口中定义的方法。
            *   它的实现 `showWithAster();` 调用了继承自 `Banner` 类的 `showWithAster()` 方法。这样，客户端调用 `printStrong()` 时，实际执行的是 `Banner` 的 `showWithAster()` 功能。
    *   通过这种方式，`PrintBanner` 将 `Banner` 类的方法“适配”到了 `Print` 接口定义的方法上。

*   **`Main.java` (客户端 - Client)**:
    *   客户端代码演示了如何使用适配器。
    *   `Print p = new PrintBanner("Hello");`:
        *   注意，客户端通过目标接口 `Print` 来引用适配器对象 `PrintBanner`。客户端并不知道（也不需要知道）`Banner` 类的存在。
        *   它传入 "Hello" 字符串给 `PrintBanner` 的构造函数。
    *   `p.printWeek();`: 客户端调用目标接口的 `printWeek()` 方法。由于多态性，实际执行的是 `PrintBanner` 类的 `printWeek()` 方法，该方法内部调用了 `Banner` 的 `showWithParen()`。输出 `(Hello)`。
    *   `p.printStrong();`: 客户端调用目标接口的 `printStrong()` 方法。实际执行的是 `PrintBanner` 类的 `printStrong()` 方法，该方法内部调用了 `Banner` 的 `showWithAster()`。输出 `*Hello*`。

**优点**:

1.  **简单直接**: 由于直接继承了被适配者，适配器可以方便地重用被适配者的方法。
2.  **效率可能较高**: 因为是直接方法调用，没有额外的委托开销。
3.  **可以覆盖被适配者的方法**: 如果需要，适配器可以重写（override）被适配者的方法（尽管在这个例子中没有这样做）。

**缺点**:

1.  **灵活性差**:
    *   **Java单继承限制**: Java不支持多重继承，所以如果 `PrintBanner` 已经继承了 `Banner`，它就不能再继承其他类了。这限制了其使用场景。
    *   **适配固定**: 类适配器在编译时就确定了要适配的类，不能动态地改变被适配者。
2.  **对被适配者有要求**: 适配器必须能够继承被适配者。如果被适配者是一个 `final` 类，则无法使用类适配器。
3.  **暴露了被适配者的所有方法**: 由于继承，适配器会继承被适配者的所有 `public` 和 `protected` 方法，即使有些方法客户端并不需要，或者适配器并不想暴露。这可能违反“最小知识原则”。
4.  **耦合度高**: 适配器和被适配者之间是继承关系，耦合度较高。

# 对象适配器模式 (Object Adapter)

对象适配器模式通过将一个被适配者类的对象组合到适配器类中（即适配器持有一个被适配者的实例），并使适配器类实现目标接口（或继承目标抽象类）来工作。客户端的请求会被适配器委托给被适配者对象进行处理。这种方式通常被称为“委托” (Delegation)。

**代码分析 (`Adaptor/Delegate/`目录下)**

*   **`Banner.java` (被适配者 - Adaptee)**:
    *   与类适配器中的 `Banner.java` 完全相同。
    *   它拥有 `showWithParen()` 和 `showWithAster()` 方法，但其接口与客户端期望的不一致。

*   **`Print.java` (目标抽象类 - Target)**:
    *   **注意**: 在这个版本的实现中，`Print` 被定义为一个 `public abstract class Print`，而不是接口。
    *   对于适配器模式的目标角色而言，它既可以是接口，也可以是抽象类，甚至是一个具体的类。客户端期望的是这个 `Print` 类型。
    *   它包含两个抽象方法，与类适配器中的 `Print` 接口定义一致：
        *   `public abstract void printWeek();`
        *   `public abstract void printStrong();`

*   **`PrintBanner.java` (适配器 - Adapter)**:
    *   这是适配器类。
    *   **实现方式**:
        *   `public class PrintBanner extends Print`
            *   它继承了目标抽象类 `Print`，因此必须提供 `printWeek` 和 `printStrong` 方法的具体实现。
        *   `private Banner banner;`
            *   **核心区别**: 它不再继承 `Banner` 类，而是持有一个 `Banner` 类的私有实例。这就是“对象适配器”中“对象”的含义——它通过组合（持有对象）来关联被适配者。
    *   构造函数 `public PrintBanner(String string)`:
        *   `this.banner = new Banner(string);`
            *   在构造函数中，它创建了一个 `Banner` 对象，并将其存储在 `banner` 字段中。
        *   `//super(string);` (代码中被注释掉的): 这行表明原作者可能考虑过或从类适配器版本修改而来，但在对象适配器中，由于不继承 `Banner`，所以不需要调用 `super(string)` 来初始化父类 `Banner` 的部分。
    *   **核心适配方法 (通过委托实现)**:
        *   `public void printWeek()`:
            *   `banner.showWithParen();`
            *   它将客户端对 `printWeek()` 的调用 **委托 (delegate)** 给了内部持有的 `banner` 对象的 `showWithParen()` 方法。
        *   `public void printStrong()`:
            *   `banner.showWithAster();`
            *   类似地，它将 `printStrong()` 的调用委托给了 `banner` 对象的 `showWithAster()` 方法。
    *   通过这种委托机制，`PrintBanner` 适配了 `Banner` 的功能以满足 `Print` 抽象类的要求。

*   **`Main.java` (客户端 - Client)**:
    *   客户端代码与类适配器版本中的 `Main.java` 非常相似。
    *   `Print p = new PrintBanner("Hello");`:
        *   客户端仍然通过目标类型 `Print`（这里是抽象类）来引用适配器对象 `PrintBanner`。客户端不直接与 `Banner` 类交互。
    *   `p.printWeek();`: 调用 `PrintBanner` 的 `printWeek`，后者委托给 `Banner` 实例的 `showWithParen`。输出 `(Hello)`。
    *   `p.printStrong();`: 调用 `PrintBanner` 的 `printStrong`，后者委托给 `Banner` 实例的 `showWithAster`。输出 `*Hello*`。

**优点**:

1.  **灵活性高**:
    *   **可以适配任何被适配者**: 由于是组合关系，适配器可以适配 `Banner` 类及其任何子类。甚至可以在运行时动态地改变被适配的对象。
    *   **不依赖继承**: 适配器类可以自由地继承其他类（除了目标抽象类 `Print`），因为它没有用掉唯一的继承机会去继承被适配者。
2.  **更好的封装性**:
    *   适配器可以选择性地暴露被适配者的方法，而不是像类适配器那样继承所有方法。这更符合“最小知识原则”。
3.  **解耦**:
    *   适配器和被适配者之间的耦合度较低，是组合/聚合关系，比继承关系更松散。
4.  **可以适配 `final` 类**: 即使 `Banner` 类是 `final` 的，对象适配器依然可以工作。

**缺点**:

1.  **实现稍复杂**: 需要额外编写委托代码，将被适配者的方法一一暴露出来。如果被适配者接口庞大，适配器代码量可能会增加。
2.  **可能有效率损失**: 存在一层间接的方法调用（委托），理论上比类适配器的直接方法调用开销稍大，但通常这种性能差异可以忽略不计。

# 如何选择？类适配器 vs. 对象适配器

在选择使用类适配器还是对象适配器时，可以考虑以下几点：

1.  **语言特性**:
    *   **类适配器**依赖于多重继承（需要同时继承被适配者类和目标接口/类）。如果编程语言不支持多重继承（如Java的类不支持多重继承，但接口可以多实现），那么类适配器的直接实现可能会受限。在Java中，类适配器通常是指适配器继承一个具体的被适配类并实现一个目标接口。
    *   **对象适配器**使用对象组合，这在任何面向对象语言中都更容易实现，且不受单继承的限制。

2.  **灵活性**:
    *   **对象适配器**通常更灵活。
        *   它可以适配一个被适配者类及其所有子类。适配器持有一个被适配者对象的引用，这个引用可以指向被适配者的任何子类实例。
        *   可以在运行时动态地更换被适配的对象。
    *   **类适配器**在编译时就固定了被适配者。

3.  **对被适配者的要求**:
    *   **类适配器**要求适配器能够继承被适配者。如果被适配者是被声明为 `final` 的类，则无法使用类适配器。
    *   **对象适配器**对被适配者没有这样的要求，即使被适配者是 `final` 类也可以适配。

4.  **代码重用和覆盖**:
    *   **类适配器**可以直接重用被适配者的方法，并且如果需要，也可以覆盖（override）被适配者的方法。
    *   **对象适配器**需要通过委托来调用被适配者的方法。如果想改变被适配者的行为，通常需要创建被适配者的子类，然后将子类对象组合到适配器中，或者在适配器中增加额外的逻辑。

5.  **透明度和封装性**:
    *   **类适配器**会继承被适配者的所有方法（public和protected），这可能会暴露一些客户端不需要的方法，降低了封装性。
    *   **对象适配器**可以选择性地只暴露被适配者的部分方法，提供更好的封装。

6.  **耦合度**:
    *   **类适配器**中适配器与被适配者是继承关系，耦合度较高。
    *   **对象适配器**中适配器与被适配者是组合/委托关系，耦合度相对较低。

**一般建议**:

*   **优先考虑对象适配器模式**。它更符合“合成复用原则”（Composite Reuse Principle），即优先使用对象组合而不是类继承。对象适配器提供了更好的灵活性和封装性。
*   只有当确实需要适配器同时具备被适配者和目标接口（或抽象类）的特性，并且语言支持（或者情况允许通过继承一个类实现一个接口的方式）时，才考虑使用类适配器。

在提供的示例中：
*   `Adaptor/Extends/` (类适配器): `PrintBanner` 继承了 `Banner` 并实现了 `Print` 接口。
*   `Adaptor/Delegate/` (对象适配器): `PrintBanner` 继承了 `Print` 抽象类并持有一个 `Banner` 的实例。这里 `Print` 是抽象类，如果 `Print` 是接口，`PrintBanner` 就可以自由继承其他类，进一步体现对象适配器的灵活性。

# 适用场景

适配器模式主要用于以下场景：

1.  **系统需要使用现有的类，而这些类的接口不符合系统的需要时。** 这是最常见的场景，即“亡羊补牢”，通过适配器来弥补接口不兼容的问题。
2.  **想要创建一个可以复用的类，该类可以与其他不相关的类或不可预见的类（即那些接口可能不一定兼容的类）协同工作。**
3.  **（仅限对象适配器）需要适配一个类的几个不同子类，但又不想为每一个子类都创建一个独立的适配器时。** 适配器可以持有被适配者抽象父类的引用，从而适配其所有子类。
4.  **需要适配一个其源代码不可修改的第三方组件或遗留系统。** 当无法直接修改旧组件以符合新接口时，适配器模式提供了一种非侵入式的解决方案。
5.  **在软件不同模块或系统之间进行集成时，如果它们的接口定义不同，可以使用适配器进行协调。**

例如：
*   在Java中，`java.io.InputStreamReader` 可以将 `InputStream` （字节流）适配成 `Reader` （字符流）。`java.io.OutputStreamWriter` 类似。
*   在GUI编程中，可能需要将不同UI库的组件适配到统一的接口下。
*   在使用外部API时，其接口可能与你的系统设计不符，可以编写适配器来封装这些API调用。

# 给Java初学者的提示

*   **核心作用：“转换”与“兼容”**: 适配器模式的核心思想就是“转换”。它像一个翻译，把一方的“语言”（接口）转换成另一方能“听懂”的“语言”（期望的接口），从而让原本无法直接沟通的双方能够协同工作。
*   **继承 vs. 组合的权衡**:
    *   **类适配器**使用**继承**，`PrintBanner extends Banner implements Print`。这意味着 `PrintBanner` *是*一个 `Banner` (获得了它的所有非私有特性)，并且*扮演*一个 `Print` 的角色。
    *   **对象适配器**使用**组合/委托**，`PrintBanner` *有一个* `Banner` 实例 (`private Banner banner;`)，并通过这个实例来完成工作。
    *   Java中类的继承是单继承的，这是一个重要的考虑因素。对象组合通常更灵活。
*   **目标可以是接口也可以是抽象类**:
    *   在 `Adaptor/Extends/` 中，目标 `Print` 是一个 `public interface Print`。
    *   在 `Adaptor/Delegate/` 中，目标 `Print` 是一个 `public abstract class Print`。
    *   对于客户端 (`Main.java`) 来说，它都是通过 `Print p = new PrintBanner(...)` 来使用的。客户端面向的是 `Print` 这个类型，不关心它是接口还是抽象类。
    *   **细微差别**:
        *   如果目标是**接口** (`Print` interface)，适配器类 (`PrintBanner`) 在实现该接口的同时，还可以自由地继承另一个有用的类（如果需要）。
        *   如果目标是**抽象类** (`Print` abstract class)，适配器类 (`PrintBanner`) 就必须继承这个抽象类，从而用掉了Java的单继承名额。这意味着 `PrintBanner` 不能再继承其他任何类了。
        *   在功能上，两者都达到了让 `PrintBanner` 能够被客户端以 `Print` 类型使用的目的。选择接口还是抽象类作为目标，取决于设计时是否期望目标类型本身带有一些具体或默认的实现（抽象类可以，接口在Java 8之前不可以有具体方法，Java 8之后可以有default方法）。在这个例子中，`Print` 抽象类没有提供任何具体实现，其作用与接口几乎等同。
*   **关注被适配者 (Adaptee)**: `Banner` 类是已经存在的、我们不希望（或不能）修改的类。适配器模式是在不改变它的前提下，让它能被新的系统所用。
*   **适配器是“中间人”**: `PrintBanner` 就是那个中间人，它理解 `Banner` 的语言，也知道如何满足 `Print` 的要求。
*   **实践出真知**: 尝试修改提供的代码，比如如果 `Adaptor/Delegate/Print.java` 是接口会怎样？如果 `Banner` 类的方法名变了，适配器如何修改？通过这些练习可以更深入地理解适配器模式。
